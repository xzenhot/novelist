# Publish service — Python dependencies
#
# DEPRECATED: consolidated into the repo-root requirements.txt.
# Install from the repo root instead:
#   pip install -r ..\..\requirements.txt

fastapi>=0.110.0
uvicorn[standard]>=0.29.0
