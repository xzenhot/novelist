# Override — Custom Human Instructions for *aravalli* (backlog planning copy)

This is the backlog planning copy of the human override, created by `/write aravalli configure poetry`. For poetry, the **operative** override command file — the one the writing agents read when producing poems — is the pipeline-root `.space/pipeline/book_aravalli/override.md` (seeded at scaffold); this backlog file is only the planning copy, kept in sync by the human.

How to use it:

- Write plain instructions, one per paragraph or bullet.
- The writing agents treat every bullet as binding and apply it to each finished poem without softening or reinterpretation.
- If the operative `## Instructions` section is empty, the agents write each poem normally and apply nothing extra.

## Instructions

_Write transformation instructions below this line. Each bullet or paragraph is binding. Leave empty to pass every poem through unchanged._